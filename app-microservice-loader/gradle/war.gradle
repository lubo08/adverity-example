apply plugin: "war"

bootWar {
    mainClassName = "com.aade.loader.LoaderApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
